apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-control-plane
  template:
    metadata:
      labels:
        app: mcp-control-plane
    spec:
      serviceAccountName: mcp-control-plane
      containers:
        - name: mcp-control-plane
          image: 508262720940.dkr.ecr.ap-northeast-1.amazonaws.com/mcp-platform/mcp-control-plane:048ee7a1cf12678ff0ac6244468efc083aa55697
          ports:
            - containerPort: 8008
          envFrom:
            - configMapRef:
                name: mcp-config
            - secretRef:
                name: mcp-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8008
            initialDelaySeconds: 5
            periodSeconds: 10
